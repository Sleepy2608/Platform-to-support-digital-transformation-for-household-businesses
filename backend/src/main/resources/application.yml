spring:
  datasource:
    url: jdbc:mysql://localhost:3306/household_db?useSSL=false&serverTimezone=UTC
    username: admin
    password: adminpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect