name: CI Pipeline

on:
  push:
    branches: [main, develop, 'feature/**', 'fix/**']
  pull_request:
    branches: [main, develop]

jobs:
  # ============================================================
  # BACKEND: Java Spring Boot + JDK 21 + Maven
  # ============================================================
  backend:
    name: Backend - Compile & Test
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Code/Server

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
          cache-dependency-path: Code/Server/pom.xml

      - name: Compile
        run: mvn compile -B

      - name: Run tests
        run: mvn test -B

  # ============================================================
  # FRONTEND: Next.js + React + TypeScript
  # ============================================================
  frontend:
    name: Frontend - Lint & Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Code/Client/src/frontend

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: Code/Client/src/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint check
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

  # ============================================================
  # AI SERVICE: Python FastAPI
  # ============================================================
  ai-service:
    name: AI Service - Lint & Test
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Code/AI

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: Code/AI/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint check
        run: pip install flake8 && flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: Run tests
        run: python -m pytest || echo "No tests found"

  # ============================================================
  # DOCKER BUILD - Kiểm tra image build được không
  # ============================================================
  docker-build:
    name: Docker Image Build Check
    runs-on: ubuntu-latest
    needs: [frontend, backend, ai-service]

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Build Docker images
        run: |
          docker compose -f docker-compose.yml build --parallel
